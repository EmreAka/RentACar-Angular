import { ColourFilterPipe } from './colour-filter.pipe';

describe('ColourFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ColourFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
