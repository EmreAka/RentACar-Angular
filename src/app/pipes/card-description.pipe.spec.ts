import { CardDescriptionPipe } from './card-description.pipe';

describe('CardDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new CardDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
