import { RentalFilterPipe } from './rental-filter.pipe';

describe('RentalFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RentalFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
